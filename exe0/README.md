# exe 0

O código atual possui três arquivos: main.c, foo.c e foo.h. A ideia é corrigir a implementação que faz uso incorreto dos arquivos .c e .h. A função foo deve estar implementada fora do main.c e ser utilizada nele.
